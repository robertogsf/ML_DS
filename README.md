1 - Selecciona un dataset de tu elección en Kaggle o cualquier otra plataforma de tu preferencia. Utilizando el dataset seleccionado, realiza las siguientes operaciones utilizando la librería Pandas o Polars:

Carga el dataset en un DataFrame. Imprime las primeras 5 filas del DataFrame. Calcula la media de una columna numérica de tu elección. Filtra las filas según una condición que consideres relevante para el análisis. Guarda el DataFrame resultante en un nuevo archivo JSON.

2 - Utilizando la librería NumPy o ndarray, realiza las siguientes operaciones:

Crea un array de tamaño 5x5 con valores aleatorios. Calcula la suma de todos los elementos del array. Calcula el promedio de cada fila y columna. Encuentra el valor máximo y mínimo del array.